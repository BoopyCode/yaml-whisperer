# YAML Whisperer

Catches YAML indentation errors before they ruin your day. Run: `python yaml_whisperer.py your-file.yaml` or check entire directories. Because debugging YAML in production is like finding a needle in a haystack... that's on fire.